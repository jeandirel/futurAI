# Scripts

- validate_mcq_jsonl.py : verifie le schema des fichiers JSONL d'annotations (MCQ, labels Bloom/SOLO, langue, etc.).
- ingestion (placeholder) : futurs scripts pour copier/valider les fichiers dans `data/raw/` et generer des index.
- annotation (placeholder) : helpers a venir pour controles de coherence avances.
